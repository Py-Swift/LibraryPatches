diff --git a/pandas/meson.build b/pandas/meson.build
index 435103a..ec842c2 100644
--- a/pandas/meson.build
+++ b/pandas/meson.build
@@ -3,14 +3,22 @@ incdir_numpy = run_command(py,
     '-c',
     '''
 import os
-import numpy as np
 try:
-    # Check if include directory is inside the pandas dir
-    # e.g. a venv created inside the pandas dir
-    # If so, convert it to a relative path
-    incdir = os.path.relpath(np.get_include())
-except Exception:
+    import numpy as np
     incdir = np.get_include()
+except Exception:
+    # Fallback for cross-compilation: numpy native modules may not load
+    # on the build machine, but we only need the header directory.
+    import importlib.util
+    spec = importlib.util.find_spec("numpy")
+    numpy_dir = os.path.dirname(spec.origin)
+    incdir = os.path.join(numpy_dir, "_core", "include")
+    if not os.path.isdir(incdir):
+        incdir = os.path.join(numpy_dir, "core", "include")
+try:
+    incdir = os.path.relpath(incdir)
+except Exception:
+    pass
 print(incdir)
      '''
   ],
diff --git a/pyproject.toml b/pyproject.toml
index d5a7f71..c8058a1 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -817,3 +817,29 @@ directory = "coverage_html_report"
 [tool.codespell]
 ignore-words-list = "blocs, coo, hist, nd, sav, ser, recuse, nin, timere, expec, expecs"
 ignore-regex = 'https://([\w/\.])+'
+
+
+[tool.cibuildwheel.ios]
+xbuild-tools = [
+  "cmake", 
+  "ninja"
+]
+# Cannot run iOS binaries on macOS build host
+test-command = ""
+# Install supplementary meson cross/native files to standard data dirs so meson
+# can find them by name, then run the normal before-build script.
+before-build = "mkdir -p ~/.local/share/meson/cross ~/.local/share/meson/native && cp {package}/ios-meson-cross.ini ~/.local/share/meson/cross/ && cp {package}/ios-meson-native.ini ~/.local/share/meson/native/ && cp {package}/ios-native-cc.sh {package}/ios-native-cxx.sh /tmp/ && chmod +x /tmp/ios-native-cc.sh /tmp/ios-native-cxx.sh && PACKAGE_DIR={package} bash {package}/scripts/cibw_before_build.sh"
+# Supplementary cross-file: exe_wrapper prevents meson from hanging on iOS binaries.
+# Supplementary native-file: ensures build-machine uses macOS system compiler,
+# not the iOS cross-compiler from the cross-venv.
+config-settings = {setup-args = ["--cross-file=ios-meson-cross.ini", "--native-file=ios-meson-native.ini"]}
+
+[tool.cibuildwheel.ios.environment]
+PIP_EXTRA_INDEX_URL = "https://pypi.anaconda.org/pyswift/simple"
+# Prefer pre-built binary wheels (esp. numpy) over source distributions,
+# since cross-compiling native deps from source for iOS is not straightforward
+PIP_PREFER_BINARY = "1"
+# Override the base LDFLAGS: -Wl,--strip-all is GNU ld-specific and not
+# supported by Apple's linker.  -g0 strips debug info at compile time.
+CFLAGS = "-g0"
+LDFLAGS = ""
\ No newline at end of file
